const today = new Date();
today.getMonth();

const person = {
    age: 30
}; //no defined type, just assures
// all properties and method defined for the object can be used

class Color {}

const red = new Color();
